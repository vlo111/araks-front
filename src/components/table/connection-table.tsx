import { Table } from 'antd';
import styled from 'styled-components';

export const ConnectionTable = styled(Table)`
  .ant-table-content {
    .ant-table-thead {
      .ant-table-cell {
        padding: 0;
      }
    }
  }
`;

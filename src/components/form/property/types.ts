export enum PropertyTypes {
  Text = 'text',
  Boolean = 'boolean',
  RichText = 'rich_text',
  Connection = 'connection',
}

export enum ConnectionPropertyTypes {
  TEXT = 'text',
  DATE = 'data',
  DATETIME = 'datetime',
  INTEGER = 'integer',
  DECIMAL = 'decimal',
}
